{
  "name": "tt-scroll",
  "version": "1.0.0",
  "description": "TikTok tarzı kaydırmalı mini oyun akışı — her kaydırmada yeni bir oyun!",
  "scripts": {
    "start": "npx http-server www -p 8080 -c-1",
    "cap:sync": "npx cap sync android",
    "android:debug": "cd android && ./gradlew assembleDebug",
    "android:release": "cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "http-server": "^14.1.1"
  }
}
